[filter "lfs"]
    process = git-lfs filter-process
    required = true
    clean = git-lfs clean -- %f
    smudge = git-lfs smudge -- %f

[push]
    default = current
    autoSetupRemote = true

[pull]
    # Use merge (not rebase) when pulling - simpler conflict resolution
    rebase = false
    # Fast-forward only - fail if not possible (forces explicit decision)
    ff = only

[rerere]
    # Remember conflict resolutions and auto-apply them
    # Still useful even with merge workflow!
    enabled = true
    autoUpdate = true

[core]
    editor = vim
    excludesfile = ~/.gitignore_global
    compression = 0
    untrackedCache = true
    # Pager: uses less with colors, works everywhere
    pager = less -RFX
    # Note: fsmonitor can cause issues on some systems, enable if needed
    # fsmonitor = true

[merge]
    # Allow merge commits (don't require fast-forward)
    ff = false
    conflictstyle = diff3

[status]
    showUntrackedFiles = all

[diff]
    tool = vimdiff
    colorMoved = default

[difftool]
    prompt = false

[difftool "vimdiff"]
    cmd = nvim -d $LOCAL $REMOTE

; [commit]
;     gpgsign = true

[color]
    ui = true

[color "branch"]
    current = yellow reverse
    local = yellow
    remote = green

[color "diff"]
    meta = 227
    frag = magenta bold
    old = red bold
    new = green bold
    commit = 227 bold
    whitespace = red reverse

[color "status"]
    added = green
    changed = yellow
    untracked = red

[color "diff-highlight"]
    oldNormal = red bold
    oldHighlight = red bold 52
    newNormal = green bold
    newHighlight = green bold 22

[alias]
    patch = !git --no-pager diff --no-color
    difff = !git --no-pager diff
    prune = fetch --prune
    stash-all = stash save --include-untracked

[gpg]
    program = gpg2

[include]
    path = .gitconfig_local

[user]
	email = isriharsha@atlassian.com
    name = Harsha Ivaturi
